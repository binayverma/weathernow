import React from 'react';

const Weather = () => (
    <div>
        This is Weather App
        <div>
           Here we are going to show report from component
        </div>
    </div>
);

export default Weather;
